<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="SAS.Views.RecognitionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="20" MinWidth="620">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Text="Face Recognition" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Text="Control camera recognition, register users, and monitor activity." Style="{StaticResource SmallLabelStyle}" />
        </StackPanel>

        <Grid x:Name="ContentGrid" Grid.Row="1" ColumnSpacing="20" RowSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="CameraColumn" Width="*" />
                <ColumnDefinition x:Name="ManagementColumn" Width="330" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid x:Name="CameraLogPanel" RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="CameraRow" Height="560" />
                    <RowDefinition x:Name="LogRow" Height="330" />
                </Grid.RowDefinitions>

                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid RowSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Camera Preview" FontSize="20" FontWeight="SemiBold" />
                            <Border Grid.Column="1" CornerRadius="6" Padding="10,4" Background="#F3F9FC">
                                <TextBlock x:Name="CameraStateTextBlock" Text="End" FontWeight="SemiBold" />
                            </Border>
                        </Grid>
                        <Border Grid.Row="1" Background="Black" CornerRadius="8">
                            <Grid>
                                <Image x:Name="CameraPreviewImage" Stretch="Uniform" />
                                <Canvas x:Name="FaceOverlayCanvas" IsHitTestVisible="False">
                                    <Border x:Name="FaceOverlayBorder" Visibility="Collapsed" BorderThickness="3" CornerRadius="4" Background="Transparent">
                                        <Border.Resources>
                                            <SolidColorBrush x:Key="RecognizedBrush" Color="#22A06B" />
                                            <SolidColorBrush x:Key="UnknownBrush" Color="#D92D20" />
                                        </Border.Resources>
                                    </Border>
                                    <Border x:Name="FaceOverlayLabelBorder" Visibility="Collapsed" CornerRadius="4" Padding="6,3">
                                        <TextBlock x:Name="FaceOverlayLabelTextBlock" Foreground="White" FontWeight="SemiBold" FontSize="11" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                                    </Border>
                                </Canvas>
                                <TextBlock x:Name="CameraPreviewPlaceholder" Text="Camera view is closed. Press Open Camera to view." Foreground="White" TextAlignment="Center" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <Grid x:Name="CameraControlsGrid" Grid.Row="2" ColumnSpacing="12" RowSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="CameraNameColumn" Width="*" />
                                <ColumnDefinition x:Name="CameraIdColumn" Width="*" />
                                <ColumnDefinition x:Name="CameraCommandColumn" Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBox x:Name="RegisterNameTextBox" Header="Name" PlaceholderText="Full name" />
                            <TextBox Grid.Column="1" x:Name="RegisterEmployeeIdTextBox" Header="NTID" PlaceholderText="A12345" />
                            <CommandBar Grid.Column="2" x:Name="ControlCommandBar" DefaultLabelPosition="Bottom" Background="Transparent" IsOpen="False" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                <CommandBar.PrimaryCommands>
                                    <AppBarButton x:Name="RegisterButton" Icon="AttachCamera" Label="Capture" ToolTipService.ToolTip="Capture face sample" Click="RegisterFaceButton_Click" />
                                    <AppBarSeparator />
                                    <AppBarButton x:Name="OpenCameraButton" Icon="Play" Label="Open Camera" ToolTipService.ToolTip="Open camera preview" Click="OpenCameraButton_Click" />
                                    <AppBarButton x:Name="CloseCameraButton" Icon="Stop" Label="Close View" ToolTipService.ToolTip="Close only this SAS camera view. JabilEye recognition keeps running." Click="CloseCameraButton_Click" />
                                </CommandBar.PrimaryCommands>
                            </CommandBar>
                        </Grid>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
                    <Grid RowSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="System Log" FontSize="20" FontWeight="SemiBold" />
                        <ListView Grid.Row="1" x:Name="FaceLogListView" IsItemClickEnabled="False" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" TextWrapping="WrapWholeWords" Margin="0,4" />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>
            </Grid>

            <Grid x:Name="ManagementPanel" Grid.Column="1">

                <Border x:Name="AuthorizedUsersCard" Style="{StaticResource CardBorderStyle}" VerticalAlignment="Stretch">
                    <Grid RowSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text="Authorized Users" FontSize="20" FontWeight="SemiBold" />
                            <TextBlock x:Name="FaceUsersSubtitleTextBlock" Text="0 active records" Style="{StaticResource SmallLabelStyle}" />
                        </StackPanel>
                        <TextBox Grid.Row="1" x:Name="UserSearchTextBox" PlaceholderText="Search employee ID" TextChanged="UserSearchTextBox_TextChanged" />
                        <ListView Grid.Row="2" x:Name="UsersListView" SelectionMode="Single" SelectionChanged="UsersListView_SelectionChanged">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="0,6" ColumnSpacing="10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="96" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding EmployeeId}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontSize="14" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                        <Button Grid.Column="2" Tag="{Binding}" Click="UserActionsButton_Click" Width="32" Height="32" Padding="0">
                                            <FontIcon Glyph="&#xE712;" FontSize="14" />
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>









